# Odoo admin password secret.
# Replace the value below before applying:
#   echo -n 'your-password' | base64
apiVersion: v1
kind: Secret
metadata:
  name: odoo
  labels:
    app.kubernetes.io/name: odoo
    app.kubernetes.io/instance: odoo
    app.kubernetes.io/component: server
type: Opaque
data:
  # base64-encoded Odoo admin password — CHANGE THIS
  odoo-password: Y2hhbmdlbWU=
